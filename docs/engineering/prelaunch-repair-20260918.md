# 출시 전 기능 복구 작업 — 2026-09-18

사용자 요청: 배포본과 일치하는 master에서 수정 목록 R01~R22를 고치고, 자동 테스트는 생략하며 재료 검색·매칭과 데이터 품질 정리 방법을 마련한다.

## 기준과 확인 방법

기준 master는 `450f9c859`, 실행 웹은 `5c140caea`이며 작업 시작 시 앱·활성 SQL 내용이 동일함을 확인했다. 원본 변경은 작업 브랜치에서 진행한다. 자동 테스트 파일을 추가·수정하거나 테스트 명령을 실행하지 않는다. 타입 검사, 빌드, 독립 코드 검토, 화면 확인과 격리 DB 적용으로 실제 확인한 범위만 기록한다.

## 변경 묶음

- R01/R02/R03/R13/R14: 최신 권한 검증 체인 보존, 두 읽기 경로 허용, 이전 요리 기록 수정·삭제·동일 요청 재생, 실측 영양 보호.
- R04/R16/R17/R18: 명시적 재로그인, 원 작성 계정만 초안 복원, 일시/인증/영구 오류 구분, 추출 작업 유지·복귀, 대기 행동 15분 제한.
- R05: 원자적 수동 등록 receipt와 결과 조회, 동일 요청 재시도, 작성자·끼니별 초안 복구 및 계정 변경 시 전송 차단.
- R09/R10/R11/R12/R15/R20: 책·상세 수량 계산 통일, 전체 내용 펼치기, 모바일 뒤로가기, 기존 장보기 이어가기, 최근 음식 등록 조건 통일, 검색 더보기.
- R19/R21 및 재료 문제: 정규화 검색·별칭·중복 등록 방지, 실제 검색 오류 전달, 팬트리 제품 삭제. 상세 데이터 품질은 [별도 문서](ingredient-search-catalog-quality-20260918.md)를 따른다.
- R06/R07/R08/R22: 작업 인증키 회전·만료 점검, 큰 DB의 스트림 백업/복원, 최신 전체·외장 백업과 실제 복구 확인, 현재 자원만 사용하는 시작 경로, 오래된 OMO 작업 비활성화.

## 운영 적용 순서

1. 정확한 full-local 컨테이너·이미지·volume을 확인한다. 전체 DB와 사진 백업, 외장 사본을 새 경로에 보존한다.
2. 현재 구조를 복제한 격리 DB에서 새 SQL을 적용한다. 실제 카탈로그 사본에서도 분류·별칭과 권한을 확인한다.
3. 운영에는 00000 → 01000 → 02000 순서로 적용하고 checksum ledger와 적용 전후 상태를 기록한다. 기존 계정/소유권 trigger를 끄지 않는다.
4. 새 웹을 별도 빌드·확인한 뒤 전환한다. DB/worker/backup 운용은 웹 전환과 별도 소유·복구 절차로 조율한다.
5. 미확인 범위와 실제 적용 상태는 아래 결과에 사실대로 기록한다.

## 확인 결과

- 자동 테스트 실행·추가·수정 없음. 타입 검사 통과, 로컬 production 빌드 통과, 독립 코드 검토의 차단 지적을 수정했다. 샘플 미리보기에서 레시피북 전체 재료 펼치기를 모바일/데스크톱으로 확인했다. 실제 소셜 로그인·새 영상 추출·모든 사용자 쓰기 흐름은 재실행하지 않았다.
- 현재 구조와 실제 카탈로그를 복사한 격리 DB에서 새 SQL 3개 적용 후, 운영에도 세 파일과 checksum 이력을 한 transaction으로 적용했다. 데이터 변경은 확인된 앞다리살 분류 1개와 별칭 2개이며 사용자 레시피·식사기록을 일괄 재작성하지 않았다. 공개·인증 사용자에게 mutation/recovery RPC 실행 권한을 추가하지 않았다.
- 실제 DB에서 공백·앞다리 별칭이 같은 ID로 매칭되고 `국간장`은 정확한 표준명을 우선함을 확인했다. 영양값 일괄 추정·대규모 재료 병합은 하지 않았다.
- 전체 DB·Auth·Storage 백업, USB 사본, 실제 격리 복원과 서명된 readiness를 새로 만들고 운영 설정 참조를 갱신했다. 대용량 문자열 제한과 행 순서 때문에 발생하던 검증 오류를 고쳤다.
- 기존 archive 외곽 v5는 유지한다. 새 manifest의 `data_semantic_format=copy-row-sha256-multiset-v1`은 행별 SHA256를 외부 정렬하되 중복 개수·relation·column 순서·non-COPY SQL까지 확인한다. 이전 marker 없는 v5는 이전 규칙 그대로 검증하고 미지원 marker는 거부한다. 이전 실제 archive 호환과 새 archive 실제 복원이 모두 확인됐다.
- 추출 credential을 generation 42로 정상 회전했다. 만료는 2026-09-25 19:32 KST이며, 6시간마다 만료 48시간 전부터 상태를 기록하는 읽기 전용 점검을 추가했다. 자동 credential 회전이나 외부 알림 발송은 없다.
- 기존 자원 7개만 다루는 출시 전 재개 경로를 설치했고 실제 launchd 실행이 PASS/exit 0, started 0/stopped 0이었다. 사라진 자원 생성·DB reset·secret 재발급은 하지 않는다. 실제 호스트 재부팅은 하지 않았다.
- 옛 OMO 작업 세 개는 백업 후 비활성화했다. 매일 백업 실행 시각은 사용자 선택 대기이며 새 일일 일정은 설치하지 않았다. 현재 24시간 백업 신선도 기준은 유지한다.

## 웹 반영 상태와 남은 확인

DB·worker·백업·시작 설정의 운영 반영은 위와 같다. 웹 화면과 API 코드의 새 빌드는 별도 웹 교체 대상이며, 이 작업 기록 작성 시 실행 웹은 기존 `5c140caea`를 유지한다. UI 변경이 이미 실서비스 화면에 반영됐다고 해석하지 않는다. 배포 시 기존 R2 운영 증거와 데이터 변경 이력을 유지하고 실제 배포 도구의 경계를 우회하지 않는다.

새 웹을 반영한 뒤 원작성 계정의 재로그인/초안 복구, 수동 레시피 저장 응답 유실 후 복구, 이전 요리 기록 수정·삭제, 제품 팬트리 삭제, 실제 영상 추출 복귀를 다시 확인한다. 현재 자료와 별개로 매일 백업 실행 시각을 확정해야 한다.

## 2026-09-19 웹 배포 완료

후속 사용자 요청으로 웹 `5d9c5b09624dff83b43d91983704cec3171087da`, build `prelaunch-5d9c5b09624d-mahAb5`를 배포했다. 이 커밋의 앱 코드는 당시 master `2da2a8474f215feb19f655958d0ec1d2afa43a5a`와 동일하며, 이미 별도로 설치한 운영 도구 변경은 다시 실행하지 않았다. 위의 웹 미반영 기록은 배포 전 상태다.

자동 테스트는 명시적으로 생략했다. production 빌드는 exit 0이며 별도 포트와 운영 포트의 `/beta`, 고유 build manifest 내용 해시, 실제 정적 파일 GET이 통과했다. 빌드 내부 ESLint는 `eslint-plugin-react-hooks`를 찾지 못해 완료되지 않았으므로 통과로 기록하지 않는다. DB는 적용된 전체 checksum 이력을 빌드 전후 읽기로 대조했으며 SQL을 재실행하지 않았다. 기존 R2 증거의 실제 해시, 마케팅 DB 구성과 보존된 권한 함수, 검토한 새 권한 함수 체인을 다시 대조했다. 기존 provider 검증 시각은 변경하지 않았다.

실제 소셜 로그인과 모든 사용자 쓰기 흐름은 이번 배포 확인에 포함하지 않았다. 영양정보 없는 실제 사용 30개 재료 정리와 매일 백업 시간 결정은 별도 남은 작업이다.

외부 도메인은 일반 HTTP 확인 요청에 403을 반환했지만 실제 브라우저에서 홈 목록과 기존 로그인 세션의 마이페이지·저장 레시피가 표시됨을 확인했다. 새 로그인/저장 요청은 실행하지 않았다.
