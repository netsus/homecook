# Acceptance Checklist

> Evidence is checked only after the owning implementation/review stage produces it. This Stage 1 document locks future tests and rollout boundaries; it does not claim migration, Storage, route, launchd, browser or remote-verifier artifacts already exist.

## Happy Path

- [x] public list/HOME/theme/search/sitemap returns only public, not-deleted recipes whose user owner is not quarantined <!-- omo:id=accept-visibility-public-read;stage=2;scope=backend;review=3,6 -->
- [x] owner can read current private detail while anon and another owner receive indistinguishable 404 <!-- omo:id=accept-visibility-private-owner;stage=2;scope=backend;review=3,6 -->
- [ ] existing Meal/shopping/session/batch/meal-log/snapshot readers resolve pinned deleted recipe history for the authorized resource owner <!-- omo:id=accept-visibility-historical-anchor;stage=2;scope=backend;review=3,6 -->
- [x] recovery activate restores eligible stored visibility without creating new owner-neutral rows or changing system content <!-- omo:id=accept-visibility-quarantine-recovery;stage=2;scope=backend;review=3,6 -->
- [x] personal upload creates a private registry object, returns object ID plus short signed URL and reissues the URL on replay <!-- omo:id=accept-image-private-upload;stage=2;scope=backend;review=3,6 -->
- [ ] recipe create attaches verified `image_object_id` and reference atomically <!-- omo:id=accept-image-object-attach;stage=2;scope=backend;review=3,6 -->
- [x] owner cancel, expired scanner and ordered drain reach terminal cleanup exactly once <!-- omo:id=accept-image-cancel-drain;stage=2;scope=backend;review=3,6 -->

## Visibility / State

- [x] private and deleted recipe/tag association never appears in `/tags`, tag filter, themes, usage count, cache, sitemap or SEO <!-- omo:id=accept-visibility-tag-matrix;stage=2;scope=backend;review=3,6 -->
- [x] `recipe_tags.visibility` is derived from the locked parent and client/direct DML cannot widen it <!-- omo:id=accept-visibility-tag-derived;stage=2;scope=backend;review=3,6 -->
- [ ] soft delete is idempotent, blocks new select/book/snapshot/start and leaves history FK/snapshot intact <!-- omo:id=accept-visibility-soft-delete;stage=2;scope=backend;review=3,6 -->
- [x] historical reader authority cannot be used to fetch general deleted/private detail or make a new plan/session <!-- omo:id=accept-visibility-history-no-bypass;stage=2;scope=backend;review=3,6 -->
- [x] quarantined user-owned profile/recipe/product/community rows are public-hidden in every direct and cached surface <!-- omo:id=accept-visibility-quarantine-hidden;stage=2;scope=backend;review=3,6 -->
- [ ] owner-null/system content is unchanged by quarantine, recovery and account cleanup <!-- omo:id=accept-visibility-neutral-preserved;stage=2;scope=backend;review=3,6 -->

## Managed Image / Concurrency

- [x] private/pending/cleanup registry row requires owner+generation; public/shared neutral row requires both null and neutral path <!-- omo:id=accept-image-owner-shape;stage=2;scope=backend;review=3,6 -->
- [x] direct anon/authenticated Storage mutation and registry/reference/outbox/quota DML are denied <!-- omo:id=accept-image-direct-mutation-denied;stage=2;scope=backend;review=3,6 -->
- [x] service-only DB reservation authority atomically locks the active generation/session, server-derived MIME/hash/size, exact request/byte/active/backlog gates and durable key/attempt/path; same-key replay or takeover does not recharge quota <!-- omo:id=accept-image-preput-reservation-authority;stage=2;scope=backend;review=3,6 -->
- [x] before PUT, `image/jpeg|image/png|image/webp`, 5MB, `10 new uploads/10min`, `100MB/24h`, active 20 and backlog 500/oldest due over 15 minutes/any dead-letter circuit breaker are enforced; replay is not recharged <!-- omo:id=accept-image-preput-gate;stage=2;scope=backend;review=3,6 -->
- [x] live same-key replay returns 202+Retry-After and expired exact CAS winner alone takes over same `upsert=false` path <!-- omo:id=accept-image-live-takeover;stage=2;scope=backend;review=3,6 -->
- [x] different payload reuse is 409 and never overwrites bytes or changes owner/generation/path <!-- omo:id=accept-image-key-reuse;stage=2;scope=backend;review=3,6 -->
- [x] late finalize after scanner/cancel/delete returns no URL and cannot revive registry or reference <!-- omo:id=accept-image-late-finalize;stage=2;scope=backend;review=3,6 -->
- [x] service-only attach authority locks the active owner/session, private recipe and exact live uploaded-unlinked object before atomically creating the thumbnail reference, transitioning `attached_private` and releasing active quota; recipe-create route wiring remains separately gated <!-- omo:id=accept-image-attach-authority;stage=2;scope=backend;review=3,6 -->
- [x] service-only manual recipe writer creates the owner-private recipe and invokes exact object-ID attach in the same DB transaction while keeping managed identity out of durable URL text; route dispatch remains separately gated <!-- omo:id=accept-image-create-attach-transaction-authority;stage=2;scope=backend;review=3,6 -->
- [x] generation-active `POST /recipes` verifies the exact session before bootstrap/write, accepts managed identity only as a UUID object ID without service-owned URL dual identity, preserves unmanaged external URL compatibility, and dispatches every manual create through the atomic writer while legacy mode cannot silently consume an object ID <!-- omo:id=accept-image-create-route-dispatch;stage=2;scope=backend;review=3,6 -->
- [x] service-only stale scanner holds the cutover fence and exact owner-generation lock, atomically lets only an expired upload lease or 24-hour unlinked grace move to one newer `cleanup_pending` generation, invalidates the upload attempt, enqueues cleanup and releases active quota once; maintenance route wiring and full concurrent race proof remain separately gated <!-- omo:id=accept-image-stale-scanner-authority;stage=2;scope=backend;review=3,6 -->
- [x] server-only stale scanner adapter submits one bounded exact scan time and accepts only unique object/outbox rows with matching positive owner/account/cleanup identities and the two authority previous states; malformed/error evidence fails closed and maintenance wiring remains separately gated <!-- omo:id=accept-image-stale-scanner-adapter;stage=2;scope=backend;review=3,6 -->
- [x] attach versus scanner/cancel has exactly one winner and any active reference blocks cleanup <!-- omo:id=accept-image-attach-race;stage=2;scope=backend;review=3,6 -->
- [x] Storage success plus DB finalize failure compensates delete or opens a newer cleanup generation <!-- omo:id=accept-image-storage-db-compensation;stage=2;scope=backend;review=3,6 -->
- [x] first 404 becomes normal-drain-excluded awaiting recheck for 15 minutes, a late object returns to pending deletion, and only an independent second 404 is verified-not-found <!-- omo:id=accept-image-first404-recheck;stage=2;scope=backend;review=3,6 -->
- [x] service-only quarantine authority claims at most 50 due first-404 rows in durable `(next_attempt_at,id)` order, persists a five-minute claim cursor, and permits pending recovery or verified absence only through the exact claimed cursor CAS; Storage probing and maintenance wiring remain separately gated <!-- omo:id=accept-image-quarantine-recheck-authority;stage=2;scope=backend;review=3,6 -->
- [x] service-only quarantine Storage adapter validates each claimed private owner/generation/path/cursor, probes only the exact object path, and submits present or independently absent evidence through the claimed CAS while indeterminate Storage and contradictory DB results fail closed; maintenance wiring remains separately gated <!-- omo:id=accept-image-quarantine-storage-adapter;stage=2;scope=backend;review=3,6 -->
- [x] service-only normal-drain claim recovers bounded due failures and expired leases before claiming only exact private reference-zero cleanup generations, while exact live-lease failure retries after five minutes and dead-letters the tenth attempt; Storage DELETE and maintenance wiring remain separately gated <!-- omo:id=accept-image-normal-drain-lease-authority;stage=2;scope=backend;review=3,6 -->
- [x] service-only normal-drain Storage adapter validates every claimed private owner/generation/path/lease, quarantines a first exact-path absence, double-authorizes a present object around the probe before exact deletion, and completes or fails only through the claimed lease CAS; maintenance wiring remains separately gated <!-- omo:id=accept-image-normal-drain-storage-adapter;stage=2;scope=backend;review=3,6 -->
- [x] service-only terminal tombstone authority claims due terminal rows in durable `(next_terminal_scan_at,id)` order with a hard 50-row `SKIP LOCKED` bound, advances 5-minute/24-hour cursors without starvation across 151 rows, and reopens only an exact owner-generation-cursor late-object observation into one newer lifecycle/outbox cleanup generation; maintenance Storage inventory wiring remains separately gated <!-- omo:id=accept-image-terminal-tombstone-authority;stage=2;scope=backend;review=3,6 -->
- [x] permanent terminal tombstone detects a later object and reopens cleanup without key/object identity loss <!-- omo:id=accept-image-terminal-late-object;stage=2;scope=backend;review=3,6 -->
- [x] terminal tombstone scanner claims at most 50 rows per tick in `(next_terminal_scan_at, id)` order with `SKIP LOCKED`, persists cursor progress, scans every 5 minutes for the first 24 hours and every 24 hours thereafter, and drains a 151-row fixture with starvation 0 <!-- omo:id=accept-image-terminal-scan-fairness;stage=2;scope=backend;review=3,6 -->
- [x] lifecycle/watermark/registry/image-idempotency compact identity is permanent; cleanup terminal + at least 90 days + terminal recheck only compacts verbose detail while preserving key/payload/result/object reference so 91-day same-key replay returns the original durable result <!-- omo:id=accept-image-compact-retention-replay;stage=2;scope=backend;review=3,6 -->
- [x] lifecycle complete requires contiguous required generations and zero nonterminal/dead-letter registry/outbox rows <!-- omo:id=accept-image-contiguous-terminal;stage=2;scope=backend;review=3,6 -->

## API / Error / Compatibility

- [x] `POST /recipes/images` accepts no public visibility intent and preserves official wrapper and image error codes <!-- omo:id=accept-image-upload-contract;stage=2;scope=backend;review=3,6 -->
- [x] owner-only `POST /recipes/images/{image_object_id}/cancel` hides missing/other-owner object as `IMAGE_NOT_FOUND` <!-- omo:id=accept-image-cancel-contract;stage=2;scope=backend;review=3,6 -->
- [x] service-owned bucket URL without verified object ID is 422 `MANAGED_IMAGE_REFERENCE_REQUIRED`; arbitrary external URL remains unmanaged compatibility input where official contract permits <!-- omo:id=accept-image-managed-reference;stage=2;scope=backend;review=3,6 -->
- [x] `IMAGE_UPLOAD_LIMITED` has positive Retry-After and does not reveal quota/backlog size <!-- omo:id=accept-image-limited-nondisclosure;stage=2;scope=backend;review=3,6 -->
- [x] private/public bucket or owner/generation mismatch is 422 `IMAGE_VISIBILITY_MISMATCH` with attach 0 <!-- omo:id=accept-image-visibility-mismatch;stage=2;scope=backend;review=3,6 -->
- [x] lifecycle maintenance/quarantine/deleting/stale-session errors are fail-closed and do not fall back to UUID-only writes <!-- omo:id=accept-image-lifecycle-errors;stage=2;scope=backend;review=3,6 -->

## Legacy / Operations

- [x] positive referenced objects alone are backfilled; unknown orphan candidates produce report/count/path hash and enqueue/delete exactly 0 <!-- omo:id=accept-image-legacy-report-only;stage=2;scope=backend;review=3,6 -->
- [ ] private and public/shared references copy to correct buckets, atomically swap references and retain old path through one compatibility release <!-- omo:id=accept-image-visibility-migration;stage=2;scope=backend;review=3,6 -->
- [ ] old path deletion is a separate irreversible gate with reference 0, read smoke, rollback floor and dead-letter 0 <!-- omo:id=accept-image-old-path-gate;stage=2;scope=backend;review=3,6 -->
- [x] expected-owner signal uses only owner_id, strict allowlisted owner path or registry owner/generation and reaches union-zero before Auth deletion <!-- omo:id=accept-image-owner-signal;stage=2;scope=backend;review=3,6 -->
- [x] service-only expected-owner signal authority counts a distinct Storage-object union from exact `owner_id`, strict allowlisted legacy owner path, or exact registry owner/account-generation/canonical private path join, while arbitrary substrings, other buckets/generations, and registry-only tombstones do not block zero; maintenance/Auth wiring remains separately gated <!-- omo:id=accept-image-owner-signal-authority;stage=2;scope=backend;review=3,6 -->
- [x] server-only expected-owner signal adapter accepts only one exact owner/account-generation authority row with safe nonnegative counts, internally consistent distinct-union evidence and matching `union_zero`; malformed/error results fail closed and maintenance/Auth wiring remains separately gated <!-- omo:id=accept-image-owner-signal-adapter;stage=2;scope=backend;review=3,6 -->
- [x] service-only Auth deletion readiness authority requires the exact cleanup lifecycle and due identity epoch, contiguous terminal cleanup generations, zero Storage nonterminal/dead-letter/generation mismatch, zero registry nonterminal/generation mismatch and expected-owner union-zero under the cutover+owner lock order; F0 claim/finalize grants, Admin deletion, maintenance wiring and lifecycle completion remain separately gated <!-- omo:id=accept-image-auth-deletion-readiness-authority;stage=2;scope=backend;review=3,6 -->
- [x] server-only Auth deletion readiness adapter accepts only one exact owner/account-generation/time authority row, safe nonnegative counts, due/terminal/dead-letter/owner-zero relationships and a server-recomputed `ready` assertion; malformed/error evidence fails closed and F0 claim/finalize grants, Admin deletion, maintenance wiring and lifecycle completion remain separately gated <!-- omo:id=accept-image-auth-deletion-readiness-adapter;stage=2;scope=backend;review=3,6 -->
- [x] service-only Auth deletion candidate authority exposes at most 50 due cleanup-pending identity epochs in durable `(next_attempt_at,id)` cursor order without claiming, deferring or mutating outbox rows, while the exact guarded claim still rechecks Storage readiness; the strict adapter, provider barrier, maintenance wiring and lifecycle completion remain separately gated <!-- omo:id=accept-image-auth-deletion-candidate-authority;stage=2;scope=backend;review=3,6 -->
- [x] server-only Auth deletion candidate adapter accepts only a bounded exact authority page with valid due identity epochs and strictly increasing `(next_attempt_at,outbox_id)` tuples after the requested cursor; malformed/error/extra-field evidence fails closed and provider deletion, maintenance wiring and lifecycle completion remain separately gated <!-- omo:id=accept-image-auth-deletion-candidate-adapter;stage=2;scope=backend;review=3,6 -->
- [x] service-only Auth deletion claim authority rechecks readiness and locks one exact owner/account-generation/identity-epoch outbox row in the same cutover/owner transaction before delegating to the existing lease claim CAS; the legacy claim/finalize grants, Admin deletion, maintenance wiring and lifecycle completion remain separately gated <!-- omo:id=accept-image-auth-deletion-claim-authority;stage=2;scope=backend;review=3,6 -->
- [x] server-only Auth deletion claim adapter accepts only an exact owner/account-generation/outbox/lease result with a valid past identity epoch, positive safe attempt and the fixed two-minute lease; malformed/error evidence fails closed and Admin deletion, finalize, maintenance wiring and lifecycle completion remain separately gated <!-- omo:id=accept-image-auth-deletion-claim-adapter;stage=2;scope=backend;review=3,6 -->
- [x] service-only Auth deletion finalize authority locks the exact generation-active owner/identity epoch and live lease/attempt before delegating to the F0 finalize CAS, records terminal identity resolution atomically on the lifecycle while retries remain unresolved, and keeps the legacy finalize grant, Admin deletion, maintenance wiring and lifecycle completion separately gated <!-- omo:id=accept-image-auth-deletion-finalize-authority;stage=2;scope=backend;review=3,6 -->
- [x] server-only Auth deletion finalize adapter submits the exact owner/account-generation/identity-epoch/live lease and accepts only the matching terminal lifecycle timestamp or unresolved retry/dead-letter result; malformed/error evidence fails closed and Admin deletion, maintenance wiring and lifecycle completion remain separately gated <!-- omo:id=accept-image-auth-deletion-finalize-adapter;stage=2;scope=backend;review=3,6 -->
- [x] server-only Auth identity lookup adapter accepts only the exact expected identity epoch, an explicit provider 404 with `user=null` as `already_absent`, or a strictly newer same-UUID epoch as `identity_replaced`; older/malformed/error evidence fails closed and `deleteUser`, maintenance wiring and lifecycle completion remain separately gated <!-- omo:id=accept-image-auth-identity-lookup-adapter;stage=2;scope=backend;review=3,6 -->
- [x] server-only Auth conditional-delete port exposes only an atomic expected-identity-epoch operation, accepts exact `deleted|already_absent|identity_replaced` evidence, and defaults to `barrier_unavailable` without calling Admin lookup/delete until a provider-supported barrier exists; concrete provider wiring, maintenance wiring and lifecycle completion remain separately gated <!-- omo:id=accept-image-auth-conditional-delete-port;stage=2;scope=backend;review=3,6 -->
- [x] Auth deletion claim, identity lookup, conditional-delete and finalize adapters preserve PostgreSQL microsecond identity epochs end to end, reject same-millisecond mismatches and invalid calendar timestamps, and keep provider deletion, maintenance wiring and lifecycle completion separately gated <!-- omo:id=accept-image-auth-epoch-precision;stage=2;scope=backend;review=3,6 -->
- [x] service-only Auth deletion drain composes one bounded 50-row candidate page with a fresh lease, exact candidate-to-claim identity epoch, atomic provider barrier and exact finalize CAS; provider errors, unavailable barriers and same-millisecond claim drift remain retry/dead-letter failures, while concrete provider and maintenance wiring stay separately gated <!-- omo:id=accept-image-auth-deletion-drain;stage=2;scope=backend;review=3,6 -->
- [x] service-only lifecycle completion authority transitions only the exact cleanup-pending generation after one terminal Auth epoch, contiguous `1..required` Storage generations, exact terminal registry/outbox pairs and expected-owner union-zero, while replay is idempotent and maintenance wiring remains separately gated <!-- omo:id=accept-image-lifecycle-completion-authority;stage=2;scope=backend;review=3,6 -->
- [x] server-only lifecycle completion adapter submits one exact owner/account-generation/time authority call and accepts only a complete row with equal safe required/completed generations, exact changed-time semantics and no extra fields; malformed/error evidence fails closed and maintenance wiring remains separately gated <!-- omo:id=accept-image-lifecycle-completion-adapter;stage=2;scope=backend;review=3,6 -->
- [x] server-only Storage maintenance phase bundle supplies only scanner, terminal tombstone scan, quarantine recheck and normal drain callbacks to the existing ordered tick, creates the delete lease only when normal drain runs, and remains blocked before expected-owner/Auth/complete without route injection or joint activation <!-- omo:id=accept-image-storage-maintenance-phases;stage=2;scope=backend;review=3,6 -->
- [x] server-only expected-owner eligibility adapter inspects every owner/generation in one bounded due Auth-candidate page through the strict signal adapter, preserves later zero-union candidates when an older account is blocked, and fails closed without route, provider, Auth drain or maintenance tick wiring <!-- omo:id=accept-image-owner-signal-eligibility-adapter;stage=2;scope=backend;review=3,6 -->
- [x] service-only eligible Auth deletion drain entry point processes only the bounded zero-union candidate partition through the existing guarded claim, atomic provider port and exact finalize CAS, reports blocked candidates without creating their leases, and remains dark-ship without a concrete provider, maintenance tick or route wiring <!-- omo:id=accept-image-eligible-auth-deletion-drain;stage=2;scope=backend;review=3,6 -->
- [x] service-only lifecycle completion candidate authority returns at most 50 exact post-Auth rows in durable `(auth_identity_deleted_at, owner_uuid, account_generation)` cursor order only when Auth terminal evidence, contiguous Storage generations, terminal registry pairs and expected-owner union-zero already match the completion authority; blocked rows do not consume the page, callers must restart from a null cursor on every maintenance pass so older rows that later become ready are re-exposed, and the function remains read-only without adapter, maintenance tick or route wiring <!-- omo:id=accept-image-lifecycle-completion-candidate-authority;stage=2;scope=backend;review=3,6 -->
- [x] server-only lifecycle completion candidate adapter accepts only a bounded exact authority page with valid past Auth deletion times and strictly increasing `(auth_identity_deleted_at, owner_uuid, account_generation)` tuples after the requested all-or-none cursor; malformed/error/extra-field evidence fails closed and each future maintenance pass must still restart from a null cursor without route or tick wiring <!-- omo:id=accept-image-lifecycle-completion-candidate-adapter;stage=2;scope=backend;review=3,6 -->
- [x] server-only lifecycle completion drain starts each bounded pass from the null candidate cursor, sends every ready-only owner/generation through the existing exact completion authority, continues later candidates after one race while failing the pass closed for alerting, and remains dark-ship without maintenance tick or route wiring <!-- omo:id=accept-image-lifecycle-completion-drain;stage=2;scope=backend;review=3,6 -->
- [x] server-only post-Storage maintenance phase bundle partitions one bounded due Auth page by exact expected-owner union-zero before consuming that pass once through the guarded eligible Auth drain and then the ready-only lifecycle completion drain; blocked owners do not stop later eligible owners, stale partitions cannot replay, and route/provider/joint activation remain separately gated <!-- omo:id=accept-image-post-storage-maintenance-phases;stage=2;scope=backend;review=3,6 -->
- [ ] MacBook tick proves `StartInterval=300`, `RunAtLoad=true`, wrong-secret 401, heartbeat gap 15 minutes, cleanup target 24 hours, alert on 3 consecutive calls failed/oldest due over 15 minutes/any dead-letter, mode 600 env or Keychain secret, JSON log 10MB × 5 rotation and next-tick recovery <!-- omo:id=accept-image-launchd-runtime;stage=2;scope=shared;review=3,6 -->

### Manual Only

- [ ] actual launchd install, production secret, power/login/sleep and external heartbeat receiver evidence remain service-owner Manual Only

## Frontend / Regression

- [ ] MANUAL_RECIPE_CREATE stores object ID, refreshes signed URL and calls owner cancel instead of browser Storage `.remove()` <!-- omo:id=accept-image-client-object-state;stage=4;scope=frontend;review=5,6 -->
- [ ] in-progress/retry/quota/expired/reference errors render without public/private selector or new layout <!-- omo:id=accept-image-client-errors;stage=4;scope=frontend;review=5,6 -->
- [ ] existing loading/empty/error/read-only/unauthorized, accessibility and return-to-action behavior remains unchanged <!-- omo:id=accept-visibility-ui-regression;stage=4;scope=frontend;review=5,6 -->
- [ ] F0 ACCOUNT_QUARANTINE and #5 RECIPE_DETAIL CTA files are not modified by this slice <!-- omo:id=accept-visibility-ui-ownership;stage=4;scope=frontend;review=5,6 -->

## Verification / Delivery

- [ ] Stage 1 runs only current docs validators, focused workflow tests, lint/typecheck, local dependency audit and diff check; GitGuardian/current-head workflows are observed separately <!-- omo:id=accept-visibility-stage1-gate;stage=2;scope=shared;review=3,6 -->
- [ ] Stage 2 writes focused tests first and records RED before migration, reader, Storage or scheduler implementation <!-- omo:id=accept-visibility-tdd-red;stage=2;scope=backend;review=3,6 -->
- [ ] focused Vitest covers read matrix, tags/cache, ACL/non-disclosure, image CAS/lifecycle and account-delete barrier <!-- omo:id=accept-visibility-vitest;stage=2;scope=backend;review=3,6 -->
- [ ] PostgreSQL existing/fresh/replay covers checks/FKs/RLS/grants/functions/triggers/partial unique and concurrent races <!-- omo:id=accept-visibility-postgres;stage=2;scope=backend;review=3,6 -->
- [ ] live Storage replay covers upload/takeover/cancel/attach/first-404/late-object/compensation with writes limited to approved test fixtures <!-- omo:id=accept-visibility-storage-live;stage=2;scope=shared;review=3,6 -->
- [ ] merged-exact-SHA remote verifier is read-only before approved cutover and records public/private role matrix plus union-zero readiness <!-- omo:id=accept-visibility-remote;stage=2;scope=shared;review=3,6 -->
- [ ] independent internal 1.5, security/DB and five-axis reviewers finish with required findings zero <!-- omo:id=accept-visibility-independent-reviews;stage=2;scope=shared;review=3,6 -->
- [ ] Draft→Ready and every started current-head check finishes success or documented normal skip before squash merge <!-- omo:id=accept-visibility-current-head;stage=2;scope=shared;review=3,6 -->
