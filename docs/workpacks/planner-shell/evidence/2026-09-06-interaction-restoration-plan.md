# 플래너 기존 조작 복원 — 2026-09-06

## 사용자 요청과 범위

사용자는 요리 계획과 실제 식사 기록을 함께 제공하는 방향을 재확인하고,
가로 스크롤 주 이동, 빈 끼니의 식사 추가, 스크롤 중 날짜 이동, 데스크톱
주간표 등 기존에 작동하던 조작의 복원을 명시적으로 요청했다.

- 기준 UI: `2a32d20bc45279bebbcd2cf9f201898f2edc658b` (`5be9534e`의 직전).
- 보존: `요리 계획 | 식사 기록`, 날짜/뒤로가기/로그인 복귀, Recipe Meal 상태,
  불변 영양 스냅샷, 식사 기록의 실제 섭취량 집계, 과거 완제품 조회·삭제.
- 복원: 3주 날짜 페이지의 가로 스크롤과 키보드 주 이동, 7일 모바일 목록,
  고정 날짜 이동 바, 날짜×끼니 데스크톱 표, 빈/채운 끼니의 추가 메뉴.
- 복원하지 않음: 계획의 영양 합계, 새 완제품 계획 생성 UI, 옛 backend writer.
- public API·DB·권한·상태 전이는 변경하지 않는다. 새로운 화면/슬라이스가
  아니라 `planner-shell`의 기존 조작에 대한 사용자 승인 회귀 복구다.

## 실행 순서

1. 기존 보호 테스트를 유지하면서 잃어버린 주 이동·추가 경로의 실패를 확인한다.
2. 기존 추가 sheet/picker/return-context를 재사용하고 주간 표시와 탐색을 복원한다.
3. 탭/날짜/로그인/legacy 삭제/에러 경계를 검증한다. 실패하거나 갱신 중인
   화면에서 새 식사 추가를 허용하지 않는다.
4. 390/320/1280px에서 이전 캡처와 비교하고 가로 주 이동, sticky, 추가·닫기·복귀를
   브라우저로 확인한다. 페이지 전체의 가로 넘침과 하단 메뉴 가림을 확인한다.
5. lint/typecheck/관련 테스트와 필요한 정적 검사를 실행하고 별도 보조 리뷰를 받는다.
6. 그 뒤 현재 영양 계산→요리 배치→섭취 기록 흐름의 검증 결과와 활성화 조건을
   구분해 보고한다. 운영 데이터 변경이나 배포·capability 활성화는 수행하지 않는다.

이 기록은 독립 Stage 5/6 또는 production activation 완료를 선언하지 않는다.
