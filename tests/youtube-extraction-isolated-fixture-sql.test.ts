import { describe, expect, it } from "vitest";
import { buildIsolatedYoutubeWorkerSyntheticFixtureSql } from "../scripts/lib/youtube-extraction-isolated-fixture-sql.mjs";
describe("isolated worker fixture SQL", () => { it("uses canonical queued user/job predicate state", () => { const value = buildIsolatedYoutubeWorkerSyntheticFixtureSql({ runIdentity: "11111111-2222-4333-8444-555555555555", userId: "22222222-2222-4222-8222-222222222222", jobId: "33333333-3333-4333-8333-333333333333", releaseSha: "a".repeat(40), schemaIdentity: "schema", allowedSnapshotDigest: "b".repeat(64), jtiHash: "c".repeat(64), nowEpoch: 2_000_000_000 }); expect(value.sql).toContain("public.users"); expect(value.sql).toContain("public.youtube_extraction_jobs"); expect(value.sql).toContain("'queued',0,3"); }); });
