export function inspectStorageRequest(
  _url: string,
  _options: { method: string },
) {
  void _url;
  void _options;
  return "safe";
}

export function remove(paths: string[]) {
  return paths.length;
}
