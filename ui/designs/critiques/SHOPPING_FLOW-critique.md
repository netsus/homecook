# SHOPPING_FLOW 설계 리뷰

> 검토 대상: `ui/designs/SHOPPING_FLOW.md`
> 기준 문서: 화면정의서 v1.5.0 § 11 / 요구사항 v1.6.3 § 1-4, 1-6
> 검토일: 2026-04-26
> 검토자: design-critic

## 종합 평가

**등급**: 🟢 통과

**한 줄 요약**: 장보기 준비 화면의 핵심 UI 상태와 인터랙션이 모두 포함되어 있으며, 디자인 토큰 적용, 모바일 UX 규칙, 요구사항 정합성을 준수한 설계입니다. 크리티컬 이슈 없이 Stage 2 진입 가능합니다.

## 크리티컬 이슈 (수정 필수)

> 없음

## 마이너 이슈 (권장 수정)

| # | 위치 | 문제 | 제안 |
|---|------|------|------|
| 1 | 전역 | "장보기 기준 인분" 문구가 반복적으로 등장 | UX copy 일관성 검토 시점에서 "합산 계획 인분" vs "장보기 기준 인분" 문구 통일 여부 재확인 권장 |
| 2 | Step D Loading | "팬트리 재료를 확인 중이에요..." 세부 진행 상태 표시 여부 | 현재는 선택사항으로 표기. 실제 구현 시 서버 처리 속도에 따라 필요 여부 결정 권장 |
| 3 | 하단 CTA 고정 | safe-area-inset-bottom 반영 명시 | 구현 단계에서 iOS 하단 safe area 처리 확인 필요 |
| 4 | 320px 대응 | 좁은 폭에서 레시피명 1행 wrap 시 카드 높이 과도 증가 가능성 | 실제 구현 후 320px viewport 확인 권장. 필요 시 말줄임 또는 폰트 크기 미세 조정 |

## 체크리스트 결과

### A. 요구사항 정합성

- [x] 화면 정의서의 모든 컴포넌트가 와이어프레임에 포함됐는가
  - Step A~D 모두 명시, 레시피 선택/해제, 인분 조정, 생성 버튼 포함
- [x] 문서에 없는 컴포넌트/필드/기능이 추가됐는가
  - 추가 없음. 모두 § 11 기준 준수
- [x] 로그인 게이트 대상 액션이 모두 처리됐는가
  - 화면 진입 시 로그인 필요, 인터랙션 노트에 모든 액션 로그인 필요 표시
- [x] read-only 상태가 올바르게 반영됐는가
  - N/A. 이 화면은 미완료 상태에서만 접근
- [x] 삭제된 엔드포인트 `DELETE /recipes/{id}/save`가 UI에 등장하지 않는가
  - 등장하지 않음

### B. 공통 상태 커버리지

- [x] Loading 상태 (스켈레톤/인디케이터) 포함
  - Step A Loading: 스켈레톤 + 안내 문구
  - Step D Loading: 인디케이터 + 진행 문구
- [x] Empty 상태 (안내 + CTA) 포함
  - "장보기 대상이 없어요" + [플래너로 돌아가기]
- [x] Error 상태 (안내 + [다시 시도]) 포함
  - "장보기 목록을 불러오지 못했어요" + [다시 시도]
- [x] read-only 상태가 필요한 화면에서 수정 UI 비노출
  - N/A. 이 화면은 read-only 적용 대상 아님

### C. 내비게이션 & 플로우

- [x] 하단 탭 4개 구조 일관성
  - 하단 탭 영향 없음. 이 화면은 플래너 진입 후 전체화면
- [x] 유저 Flow맵과 진입/이탈 경로 일치
  - 진입: PLANNER_WEEK → [장보기]
  - 이탈: SHOPPING_DETAIL 자동 이동 (Step D 완료 후)
  - Empty: PLANNER_WEEK 복귀
  - 유저 Flow맵 § ④ 장보기 여정과 일치
- [x] 뒤로가기 동작 명시 여부
  - 상단 앱바에 "← 뒤로" 명시
  - Step D 진행 중 뒤로가기 차단 (선택) 명시
- [x] 플로우 단절 지점 없는가
  - 없음. 모든 경로 명확

### D. UX 품질

- [x] 터치 타겟 최소 44px 준수
  - 체크박스: 44×44px
  - 스테퍼 버튼: 44×44px
  - 하단 CTA: 48px 높이 (충분)
- [x] 모바일 퍼스트 (375px 기준) 레이아웃
  - 와이어프레임 상단에 "375px (모바일 기준)" 명시
- [x] 작은 모바일 sentinel에서도 구조가 유지되는가
  - ⚠️ 레시피명 1행 wrap 시 카드 높이 증가 가능성 → 마이너 이슈 4번
  - 전체 구조는 유지됨
- [x] 핵심 액션이 시각적으로 명확한가
  - [장보기 목록 만들기]: `--brand`, 하단 고정, 높이 48px
  - 체크박스/스테퍼: `--olive` (서브 액션)
  - CTA 위계 명확
- [x] whole-page horizontal scroll을 유도하지 않는가
  - 가로 스크롤 없음
- [x] scroll containment가 명확한가
  - 명시: "카드 리스트만 세로 스크롤 (앱바, helper copy, 하단 CTA는 고정)"
- [x] 장보기 D&D (sort_order) UI가 명확한가
  - N/A. 이 화면은 SHOPPING_FLOW (생성 전). D&D는 SHOPPING_DETAIL에서 처리
- [x] 팬트리 제외 섹션 2-영역 구조가 올바른가
  - N/A. 2-영역 구조는 SHOPPING_DETAIL
- [x] AI스러운 제네릭 UI 사용 여부
  - 없음. 카드 기반 실용적 레이아웃

### E. 도메인 규칙 정합성

- [x] `meals.status` 전이 표현 정확성
  - 이 화면은 `status='registered' AND shopping_list_id IS NULL` 식사 대상
  - 정확히 반영됨
- [x] 독립 요리는 meals 상태를 바꾸지 않는 것이 UI에서 명확한가
  - N/A. 이 화면은 독립 요리와 무관
- [x] 팬트리는 수량이 아닌 보유 여부만 표시
  - 팬트리 표시는 SHOPPING_DETAIL에서 처리. 이 화면은 N/A
- [x] 요리 모드에서 인분 조절 UI가 없는가
  - N/A. 요리 모드 아님
- [x] 저장 가능한 레시피북 타입만 표시
  - N/A. 레시피북 무관

### F. 디자인 토큰 준수

- [x] CTA 버튼에 `--brand (#FF6C3C)` 사용됐는가
  - 하단 CTA: `--brand` 명시
- [x] 카드 배경에 `--surface (#ffffff)` 명시됐는가
  - 레시피 카드 배경: `--surface` 명시
- [x] 태그·칩에 `--olive (#2ea67a)` 사용됐는가
  - 체크박스 활성: `--olive`
  - 인분 조정 버튼: `--olive` 테두리
- [x] 보조 텍스트에 `--muted (#999999)` 사용됐는가
  - helper copy: `--muted`
  - 보조 정보 (합산 계획 인분): `--muted`
- [x] 카드 border-radius 16px 준수됐는가
  - 명시: 카드 border-radius `16px`
- [x] 수평 여백 16px(모바일) 기준 준수됐는가
  - 하단 CTA 좌우 수평 여백 `16px` 명시
- [x] 확정 토큰 외 임의 색상이 사용됐는가
  - 없음. 모든 색상이 확정 토큰 기준

## design-generator 재작업 요청 항목

> 🟢 통과 — 재작업 불필요

## 통과 조건

이 화면 설계가 구현으로 넘어가려면:
- [x] 크리티컬 이슈 0개
- [x] 마이너 이슈 처리 또는 수용 결정
  - 마이너 이슈 4개는 모두 구현 단계 또는 실제 동작 확인 시점에서 검증 가능한 항목
  - 설계 단계에서 차단 사유 아님

---

## 상세 검토 노트

### 강점

1. **완전한 상태 커버리지**
   - Loading (Step A, Step D), Empty, Error 모든 상태 포함
   - 각 상태별 사용자 안내와 다음 행동 명확

2. **명확한 디자인 토큰 적용**
   - 색상, 간격, border-radius 모두 확정 토큰 기준
   - `olive base + thin orange highlight` 방향 준수

3. **모바일 UX 규칙 준수**
   - 터치 타겟 44px
   - 가로 스크롤 없음
   - scroll containment 명시
   - CTA 위계 명확

4. **요구사항 정합성**
   - 화면정의서 § 11 Step A~D 모두 포함
   - `status='registered' AND shopping_list_id IS NULL` 조건 정확
   - 유저 Flow맵 § ④ 장보기 여정과 완벽 일치

5. **디자인 결정 사항 문서화**
   - 레시피 카드 세로 배치 이유 명확
   - 토큰 적용 방향 설명
   - Empty 상태 CTA 선택 근거 제시

### 마이너 개선 제안

1. **UX copy 일관성 (마이너 1)**
   - "합산 계획 인분" vs "장보기 기준 인분" 문구가 혼재
   - 설계 단계에서는 문제 없으나, 실제 구현 시 copy 일관성 검토 권장

2. **세부 진행 상태 표시 (마이너 2)**
   - Step D Loading에서 "팬트리 재료를 확인 중이에요..." 표시는 선택사항
   - 서버 처리 속도에 따라 구현 단계에서 결정 권장

3. **Safe area 처리 (마이너 3)**
   - iOS 하단 safe area 처리는 구현 단계 체크리스트 항목
   - 설계에 명시적으로 언급되진 않았으나, 하단 CTA 고정 시 필수

4. **320px 대응 (마이너 4)**
   - 레시피명 1행 wrap 시 카드 높이 증가 가능성
   - 설계자가 이미 검토 필요 항목으로 인지 (라인 312~314)
   - 실제 구현 후 확인 권장

### 검증 완료 항목

- ✅ 화면정의서 § 11 매핑 테이블 완비
- ✅ 인터랙션 노트에 모든 액션 트리거/결과 명시
- ✅ Scroll Containment 명시 섹션 포함
- ✅ Mobile UX Blocker 체크 테이블 포함
- ✅ 디자인 결정 사항 5개 항목 문서화
- ✅ design-critic 검토 필요 항목 7개 사전 명시

### 결론

이 설계는 요구사항, UX 품질, 디자인 토큰 적용, 도메인 규칙 정합성 모든 축에서 통과 기준을 만족합니다. 마이너 이슈 4개는 모두 구현 단계 또는 실제 동작 확인 시점에서 검증 가능한 항목이며, 설계 자체의 차단 사유가 아닙니다.

**Stage 2 진입 가능합니다.**
