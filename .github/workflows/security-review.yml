name: Security Review

permissions:
  contents: read

on:
  pull_request:
  push:
    branches:
      - main
      - master
  schedule:
    - cron: "0 3 * * 1"
  workflow_dispatch:

jobs:
  scope:
    runs-on: ubuntu-latest
    outputs:
      dependency_audit: ${{ steps.filter.outputs.dependency_audit }}
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0

      - name: Resolve security review path filters
        id: filter
        run: node scripts/ci-path-filter.mjs

  dependency-audit:
    needs: scope
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed when scope resolution failed
        if: needs.scope.result != 'success'
        run: |
          echo "Security review scope resolution did not succeed." >&2
          exit 1

      - name: Report not applicable
        if: needs.scope.result == 'success' && needs.scope.outputs.dependency_audit != 'true'
        run: echo "N/A - no dependency-audit paths changed."

      - name: Checkout
        if: needs.scope.outputs.dependency_audit == 'true'
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Setup pnpm
        if: needs.scope.outputs.dependency_audit == 'true'
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6
        with:
          version: 10.32.1

      - name: Setup Node
        if: needs.scope.outputs.dependency_audit == 'true'
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        if: needs.scope.outputs.dependency_audit == 'true'
        run: pnpm install --frozen-lockfile

      - name: Audit dependencies
        if: needs.scope.outputs.dependency_audit == 'true'
        run: pnpm audit --audit-level high

  snyk:
    needs: scope
    if: needs.scope.result == 'success' && needs.scope.outputs.dependency_audit == 'true' && github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Setup pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6
        with:
          version: 10.32.1

      - name: Setup Node
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Snyk
        uses: snyk/actions/node@12140f4059e244892ae643824a95459a102120dd # current
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=high
